{
  "name": "cmml-market-scan",
  "version": "1.0.0",
  "description": "CMML Market Scan project with React frontend and Node.js backend",
  "main": "index.js",
  "scripts": {
    "build": "cd web-react && npm install && npm run build",
    "start": "cd web-react && npm start",
    "dev": "cd web-react && npm run dev"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
